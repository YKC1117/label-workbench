const zlib=require('zlib');
const BASE='https://www.bartendersoftware.com/resources/library/';
const SLUGS=['gtl-a5','amazon-barcode-packing-slip-data-matrix','material-label','produce-tracking','retailfoodlabel','asn-shipping-label','ford-loadlist','ford-gtl-mixed-master','heb-seafood-case-label','hibcc-primary-and-secondary-data'];
function scanUtf16(buf){const out=[];for(let i=0;i+4<=buf.length;i++){if(buf[i]!==0xff||buf[i+1]!==0xfe||buf[i+2]!==0xff)continue;let chars=0,head=4;if(buf[i+3]===0xff){if(i+6>buf.length)continue;chars=buf[i+4]|buf[i+5]<<8;head=6}else chars=buf[i+3];if(chars<1||chars>10000)continue;const s=i+head,e=s+chars*2;if(e>buf.length)continue;const text=buf.subarray(s,e).toString('utf16le');if(text&&!/[\x00-\x08\x0b\x0c\x0e-\x1f]/.test(text))out.push({offset:i,text});i=e-1}return out}
function scanTags(buf){const out=[];for(let i=0;i+8<buf.length;i++){if(buf[i]!==0xff||buf[i+1]!==0xff||buf[i+2]!==1||buf[i+3]!==0)continue;const len=buf[i+4]|buf[i+5]<<8;if(len<3||len>80||i+6+len>buf.length)continue;let ok=true;for(let j=0;j<len;j++){const b=buf[i+6+j];if(b<0x20||b>0x7e){ok=false;break}}if(!ok)continue;const type=buf.subarray(i+6,i+6+len).toString('ascii');if(/Data$/i.test(type))out.push({offset:i,type})}return out}
function ownerFor(tags,offset){let hit='';for(const t of tags){if(t.offset<=offset)hit=t.type;else break}return hit}
async function inspect(slug){const page=await fetch(BASE+slug,{headers:{'User-Agent':'LabelWorkbench/1.0'}});if(!page.ok)return{slug,error:`page ${page.status}`};const html=await page.text(),id=/data-download\s*=\s*["']?(\d+)/i.exec(html)?.[1];if(!id)return{slug,error:'no data-download'};const r=await fetch(`https://www.bartendersoftware.com/download-resource?resourceId=${id}`,{redirect:'follow',headers:{'User-Agent':'LabelWorkbench/1.0','Accept':'application/octet-stream,*/*'}});if(!r.ok)return{slug,id,error:`resource ${r.status}`};const file=Buffer.from(await r.arrayBuffer()),head=file.subarray(0,1800).toString('latin1').replace(/\0/g,'');if(!head.includes('Bar Tender Format File'))return{slug,id,error:'not BTW'};const marker=Buffer.from([0x49,0x45,0x4e,0x44,0xae,0x42,0x60,0x82,0,1]),at=file.indexOf(marker);if(at<0)return{slug,id,error:'container marker missing'};let c;try{c=zlib.inflateSync(file.subarray(at+marker.length))}catch(e){return{slug,id,error:'inflate '+e.message}}const entries=scanUtf16(c),roots=entries.filter(e=>e.text.startsWith('Root.MasterSelectedObject.')),tags=scanTags(c),instances=[];for(let i=0;i<roots.length;i++){const root=roots[i],start=Math.max(0,root.offset-20),end=i+1<roots.length?Math.max(start,roots[i+1].offset-20):c.length,strings=entries.filter(e=>e.offset>=root.offset&&e.offset<end),name=strings.find((e,j)=>j>0&&e.text&&!e.text.startsWith('Root.MasterSelectedObject.'))?.text||'';instances.push({name,owner:ownerFor(tags,start),root:root.text})}const textInstances=instances.filter(x=>/^(?:Text|文字)\s*\d+/i.test(x.name)&&!/^Text Input Box/i.test(x.name)),barcodeInstances=instances.filter(x=>/^(?:Barcode|條碼)\s*\d+/i.test(x.name)),c128=barcodeInstances.filter(x=>x.owner==='BcC128Data'),dm=barcodeInstances.filter(x=>x.owner==='BcDatamatrixData');const version=/Application:\s*Version=([^;\r\n]+)/i.exec(head)?.[1]?.trim()||'',compat=/Document:\s*CompatibleVersion=([^;\r\n]+)/i.exec(head)?.[1]?.trim()||'';return{slug,id:Number(id),version,compat,bytes:file.length,roots:roots.length,textInstances:textInstances.length,barcodeInstances:barcodeInstances.length,c128Instances:c128.length,dmInstances:dm.length,textNames:textInstances.slice(0,50).map(x=>x.name),barcodeNames:barcodeInstances.map(x=>`${x.name}:${x.owner}`),score:textInstances.length+c128.length*20+dm.length*20}}
(async()=>{const rows=[];for(const slug of SLUGS){try{rows.push(await inspect(slug))}catch(e){rows.push({slug,error:e.message})}}rows.sort((a,b)=>(b.score||0)-(a.score||0));console.log(JSON.stringify(rows,null,2));const winners=rows.filter(x=>!x.error&&x.textInstances>=10&&(x.c128Instances>=2||x.dmInstances>=1));console.log('WINNERS',JSON.stringify(winners,null,2));console.log(`PASS: instance-level scan complete; strong donors=${winners.length}`)})().catch(e=>{console.error(e);process.exit(1)});
